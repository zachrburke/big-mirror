import { Component, h, ComponentProps } from "preact";

export default class Calender extends Component {
    render() {
        return (
            <div>
                <h1>Calender</h1>
            </div>
        );
    }
}